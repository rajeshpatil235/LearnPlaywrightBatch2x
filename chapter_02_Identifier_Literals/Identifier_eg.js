// ✅ Valid Identifiers
let userName;        // Camel case (recommended for JS)
let _privateVar;     // Can start with underscore
let $element;        // Can start with dollar sign
let num2;            // Can contain numbers (but not start with)
let total_score;     // Snake case (common in Python/DB)
let MAX_SIZE;        // ALL_CAPS for constants
let student1;        // Letters + numbers
// ❌ Invalid Identifiers
let 1stPlace;        // Cannot start with number
let my-var;          // Hyphens not allowed
let my var;          // No spaces allowed
let let;             // Cannot use reserved keywords
let class;           // Reserved keyword
let @email;          // Special characters not allowed (except _ and $)
let #tag;            // Not valid (only in specific contexts)
// 📋 Rules Summary
Rule
// Must start with letter, _, or $
// Can contain letters, numbers, _, $
// Case-sensitive
// No spaces or hyphens
// Cannot be reserved words
// 💡 Best Practices
// Use meaningful names
let a;           // Bad
let userAge;     // Good
// Use camelCase in JavaScript
let firstName;   // Good
let first_name;  // Okay but not JS convention
// Constants in UPPER_CASE
const API_KEY = "abc123";
const MAX_USERS = 100;
// These rules apply to most C-style languages (JavaScript, Java, C++, C#). 🎯
// These rules apply to most C-style languages (JavaScript, Java, C++, C#). 🎯
// These rules apply to most C-style languages (JavaScript, Java, C++, C#). 🎯
// These rules apply to most C-style languages (JavaScript, Java, C++, C#). 🎯
// These rules apply to most C-style languages (JavaScript, Java, C++, C#). 🎯
// These rules apply to most C-style languages (JavaScript, Java, C++, C#). 🎯
// These rules apply to most C-style languages (JavaScript, Java, C++, C#). 🎯

//alt+shift+down arrow