let age = "rajesh"; //string literal assigned to the variable age
let isTrue = true; //boolean literal assigned to the variable isTrue
let a = 90;//number literal assigned to the variable a
let pi = 3.142; //number literal assigned to the variable pi
let name = "Rajesh";  //string literal assigned to the variable name

let nullValue = null; //null literal assigned to the variable nullValue
let undefinedValue; // the variable is declared but not assigned, so it has the value undefined